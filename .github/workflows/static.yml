name: static check
on: pull_request

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@master
    - name: check
      uses: grandcolline/golang-github-actions@master
      with:
        run: lint
        token: ${{ secrets.GITHUB_TOKEN }}
